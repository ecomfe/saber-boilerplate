/**
 * @file ${name}
 * @author ${author}(${email})
 */

define(function (require) {

    var config = {};

    config.view = require('${view}');

    config.model = require('${model}');

    return config;

});
